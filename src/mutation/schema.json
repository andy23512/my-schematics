{
  "$schema": "http://json-schema.org/schema",
  "id": "graphql-service-schematics-query",
  "title": "GraphQL Service Schematic mutation schematic",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the mutation service.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the service.",
      "visible": false
    },
  },
  "required": ["name"]
}